/** Default seller/platform commission — 2% per CLAUDE monetization rules. */
export const DEFAULT_PLATFORM_FEE_PERCENT = 2;
export const DEFAULT_BUYER_FEE_PERCENT = 0;
export const DEFAULT_WITHDRAWAL_FEE_PERCENT = 0;
export const DEFAULT_ORDER_CURRENCY = 'UAH';

export const DEFAULT_AUTO_CONFIRM_DAYS = 7;
export const DEFAULT_PAYOUT_HOLD_DAYS = 3;
export const DEFAULT_RETURN_WINDOW_DAYS = 14;

/** Background job interval — 1 hour. */
export const ORDER_JOB_INTERVAL_MS = 60 * 60 * 1000;
